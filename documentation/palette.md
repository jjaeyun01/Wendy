# Wendy — Palette (`config/palette.py`)

**Canonical API, color indices, consumers, and `make_palette` behavior:** `documentation/config-style.md` → **Palette**.

This file exists so searches for “palette” still land here; do not duplicate those details in other docs.
