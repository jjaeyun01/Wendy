# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
sounddevice
numpy
opencv-python
vosk
# mediapipe   # broken on Python 3.13 — motion_detector.py needs Python ≤3.12
#
# PyTorch (CNN clap classifier): not on PyPI for every Python/macOS combo yet.
# If you need torch, use a Python 3.12 venv and: pip install -r requirements-ml.txt
