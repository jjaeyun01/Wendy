# ML / CNN path: use Python 3.11 or 3.12 (PyTorch wheels), then:
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt && pip install -r requirements-ml.txt
-r requirements.txt
torch
